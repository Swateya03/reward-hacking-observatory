# Layer 0 requirements
# Run: pip install -r requirements.txt

# Workspace server
fastapi==0.115.0
uvicorn[standard]==0.30.0

# Agent
anthropic>=0.40.0
httpx>=0.27.0
python-dotenv>=1.0.0

# Testing
pytest>=8.0.0

# Visualization
matplotlib>=3.8.0
numpy>=1.26.0

# Later layers (commented out — install when needed)
# openai>=1.50.0          # Layer 2: GPT-4o client
# google-generativeai     # Layer 2: Gemini client
# transformers>=4.40.0    # Layer 6: Qwen training loop
# torch>=2.0.0            # Layer 6: REINFORCE
# prometheus-client       # Layer 8: monitoring
# rich                    # Layer 7: annotation CLI
# click                   # Layer 7: annotation CLI
